# YouTube-Watch-Reminder---Chrome-Extension
Build a Chrome extension (Manifest V3) that adds an "Add to Queue" button on YouTube video pages, lets users set priority and due dates, stores the queue via Chrome sync storage, and sends daily browser notifications for pending videos
